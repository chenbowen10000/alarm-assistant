alarm:
  models:
    primary:
      timeout: 30s
    fallback:
      timeout: 30s

logging:
  level:
    com.example.alarm: DEBUG

resilience4j:
  ratelimiter:
    instances:
      analyzeApi:
        limit-for-period: 100
        limit-refresh-period: 60s

management:
  endpoints:
    web:
      exposure:
        include: "*"
