alarm:
  models:
    primary:
      provider: deepseek
      api-key: test-key
      base-url: https://api.deepseek.com
      model: deepseek-chat
      temperature: 0.3
      max-tokens: 2000
      timeout: 30s
    fallback:
      provider: qwen
      api-key: test-key
      base-url: https://dashscope.aliyuncs.com/compatible-mode
      model: qwen-turbo
      temperature: 0.3
      max-tokens: 2000
      timeout: 30s
  pipeline:
    mode: single
